# Frozen-selection private experiment — 19 September 2026

## Outcome

The reviewed frozen-selection experiment completed privately. The zero-fade reproduction gate
passed before the unchanged ageing/cost case ran. Both retained studies, manifests, physical
state paths, cash flows and dated NPVs independently reconcile. This record publishes no aggregate
figure or outcome sign; publication remains separately gated and the shipped selection policy is
unchanged.

The evidence remains retrospective supplementary research over one already inspected historical
window. It is not expected revenue, a forecast, financial advice, a bankable study or evidence
that either selection objective will remain preferable.

## Authorization and execution identity

- Integration PR #83 merged as `d57125e`; merge CI `35316480803` passed on Python 3.12 and 3.13.
- Declaration PR #84 merged as `4d051ef`; merge CI `35444486512` passed on Python 3.12 and 3.13.
- The operator instructed the private experiment to proceed and then instructed continuation.
  Neither instruction authorizes public aggregate publication.
- Declaration SHA-256:
  `178ed1903a6072acc3a7c0ca95b62aedbeb46e5ac6878ecd9e92c1e9c468819f`.
- Executing commit: `3c69ffae2aac610a7670fa0febe32c157f45678f`, the reviewed declaration
  head whose package source tree is pinned to implementation commit `d57125e`.
- Runtime: Python 3.12.14, NumPy 2.5.3, pandas 2.3.3, SciPy 1.18.1 and
  scikit-learn 1.9.1. The full platform and package inventory is retained privately.

## Gated execution and reconciliation

The declaration and complete package tree verified before prices were read. Official-history
custody reported six matching files and zero differences. The accepted Stage 9 index, every file
it names, the frozen selection seal, candidate forecast identity, original selected columns and
complete evaluation calendar all verified.

The zero-fade study ran first. Independent fixed-battery dispatch reproduced each accepted
selection on all 329 days. Daily cash margin and grid-flow differences remained inside the fixed
absolute `1e-6` tolerances, and unrounded aggregate margin and flow totals remained inside the
fixed `329e-6` tolerances. Capacity stayed constant, terminal energy reconciled and operating
costs were zero. No tolerance changed after inspection.

Only after that pass did the ageing/cost study run. Each arm retained its own state across all
329 days and 31,584 native quarter-hour intervals. Independent checks covered calendar identity,
daily energy balance, cycle limits, cell/grid throughput, calendar and cycle fade, capacity
continuity, terminal energy, daily-to-summary cash margin, fixed and variable operating costs,
undiscounted project cash flow and dated NPV. The recorded paired cash-margin, NPV, cell-EFC,
final-capacity and undiscounted-cash-flow differences all reconcile exactly to independently
calculated differences. Their values and signs remain in the private bundle.

Both manifests verify. Re-rendering each manifest reproduces its original HTML byte for byte.
The index JSON files differ only because each render records its own render time and output path;
the deterministic report payload is the HTML artifact.

## Private retention

The ignored directory `private/frozen-selection-experiment-2026-09-19/` contains declaration and
configuration copies, runtime inventory, custody and selection identities, the zero-fade
reproduction comparison, independent ageing/cost reconciliation, both studies' daily, strategy,
cash-flow, summary and manifest files, and original and re-rendered reports. No raw or processed
price data or forecast table is copied into this experiment bundle.

The evidence index seals 33 retained files. Its SHA-256 is
`f7cb1203898844a1539c0dd63e28415ac96252753b0c5c5c150b755f09ae4a4c` and it records
`publication_authorized` as false. All indexed hashes verify. Local retention is not independent
durable custody.

## Validation and change scope

This continuation changes documentation only. It adds no analytical behavior, dependency,
configuration, source, model family, forecast fit or selection rule. Existing implementation and
declaration CI already passed on Python 3.12 and 3.13. The private execution supplies the declared
official-history measurement and independent reconciliation; it is not a reason to weaken or
replace existing synthetic regression coverage.

Review includes the complete documentation diff, local-link checks, whitespace, credential and
generated-file scans, contributor-neutral wording, and confirmation that `private/` remains
ignored and the Git worktree contains no generated study output.

## Next gate

Review the private aggregate and decide separately whether to authorize publication. If
authorized, publish only an aggregate record carrying the retrospective label, declared horizon,
physical and finance assumptions, both signed outcomes and standing exclusions, then verify the
deployed bytes. Do not publish raw prices, forecasts or daily schedules, and do not treat a
favourable or unfavourable sign as an acceptance criterion.
